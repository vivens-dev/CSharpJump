var message = Console.ReadLine(); 
Console.WriteLine($"Yo {message}");
string animal = "Bear";
Console.WriteLine(animal + "is amazing."); // an expression
int x = 5; // declaration and initialization

int y; // declaration 
y = 5; // initialization

Console.WriteLine($"The number is {y}");

if(y == 5) { 
   int z = 10;
}
// Outside of the if block, the variable z does not exist

int? v = null; // Nullable type

Console.WriteLine($"The value of v is {v}");



// variable - stores some value
// identifier - what we call the variable
// Expression - evaluates to a value
// Operator - it works on operands
// Operands - The thing(s) that the operator works on
// literal - the fixed value which can be used by variables
// Variable Declaration and Initialization - C# will give you an error if you try to use a variable which has not been initialized
// Side note: most of Computer Science is vocabulary, to step up your game, LEARN THE VOCABULARY -> SPEAK THE RIGHT LANGUAGE