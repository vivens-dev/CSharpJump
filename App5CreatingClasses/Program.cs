using App5CreatingClasses;

Person person = new Person();
person.FirstName = "Vivens";
person.LastName = "Ndatinya";

Console.WriteLine(person.GetFullName());

Console.WriteLine(person.FullName);
